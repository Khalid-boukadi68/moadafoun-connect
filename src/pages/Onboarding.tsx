import React, { useState } from 'react';\nimport './Onboarding.css';\n\nconst Onboarding = () => {\n    const [currentStep, setCurrentStep] = useState(0);\n    const steps = [\n        { title: 'Welcome!', content: 'This is the first step of onboarding.' },\n        { title: 'Connect Your Account', content: 'Let’s get you connected.' },\n        { title: 'Customize Your Settings', content: 'Make it yours!' },\n        { title: 'All Set!', content: 'You are ready to go.' }\n    ];\n\n    const nextStep = () => {\n        setCurrentStep(prevStep => Math.min(steps.length - 1, prevStep + 1));\n    };\n    const prevStep = () => {\n        setCurrentStep(prevStep => Math.max(0, prevStep - 1));\n    };\n\n    return (\n        <div className='onboarding-container'>\n            <h2>{steps[currentStep].title}</h2>\n            <p>{steps[currentStep].content}</p>\n            <div className='buttons'>\n                <button onClick={prevStep} disabled={currentStep === 0}>Back</button>\n                <button onClick={nextStep}>{currentStep === steps.length - 1 ? 'Finish' : 'Next'}</button>\n            </div>\n        </div>\n    );\n};\n\nexport default Onboarding;\n